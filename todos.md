- set up environment
  - make state deepcopy-able 
  - game:
    - get_state (it's simple just return the state)
  - state:
    - implement __deepcopy__
    - implement get_state_after_action, need a deepcopy of current state
  - Monte Carlo Tree Search
    - implement MCTSNode (components: state, last_action, children, visits?, wins)
      - state: CarcassonneGameState
      - last_action: Action
      - children: dict[action, MCTSNode]
      - visits: int
      - wins: int (lose is visits - wins)
    - implement MCTSAgent (components: game)
      - game: CarcassonneGame
      - 